const Suggestion = ({videoId}) => {
  // const suggestedVideos=useFetchSuggestedVideos(videoId);
  // console.log("suggested videos :",suggestedVideos);
  return (
    <div className='border-2 rounded-xl w-[20%]'>
      Suggestion
    </div>
  )
}

export default Suggestion